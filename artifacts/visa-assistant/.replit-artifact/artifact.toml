kind = "web"
previewPath = "/"
title = "Visa Assistant"
description = "Turkey Travel Visa Assistant — country passport picker with visa/e-Visa cards and admin panel."
version = "1.0.0"
id = "artifacts/visa-assistant"
router = "path"

[[services]]
name = "web"
paths = [ "/", "/visa/" ]
localPort = 21735

[services.development]
run = "pnpm --filter @workspace/visa-assistant run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/visa-assistant", "run", "build" ]
serve = "static"
publicDir = "artifacts/visa-assistant/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21735"
BASE_PATH = "/"
